name: treat_decider
description: Treat Decider feature - randomly selects team members to treat the team.
resolution: workspace
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  widgets:
    path: ../widgets
  bloc: ^9.1.0
  flutter_bloc: ^9.1.1
  hive: ^2.2.3
  equatable: ^2.0.7
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
