name: example_app
description: "Example Flutter app demonstrating widgets package with Clean Architecture."
publish_to: 'none'
resolution: workspace
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  design_system:
    path: ../../packages/core/design_system
  auth:
    path: ../../packages/features/auth
  todo:
    path: ../../packages/features/todo
  flutter_riverpod: ^2.5.0
  go_router: ^17.0.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
