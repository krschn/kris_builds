name: example_app
description: "Example Flutter app demonstrating widgets package with Clean Architecture."
publish_to: 'none'
resolution: workspace
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  widgets:
    path: ../../packages/widgets
  auth:
    path: ../../packages/auth
  todo:
    path: ../../packages/todo
  treat_decider:
    path: ../../packages/treat_decider
  bloc: ^9.1.0
  flutter_bloc: ^9.1.1
  go_router: ^17.0.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  equatable: ^2.0.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  patrol: ^4.1.0

flutter:
  uses-material-design: true
